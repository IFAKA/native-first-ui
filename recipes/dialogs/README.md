# Dialogs

Use `dialog`, `show-modal`, Escape, and `form method="dialog"`. `commandfor` is the declarative modern path; provide a small feature-detected fallback only when targeting browsers without invoker commands.
