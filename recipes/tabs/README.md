# Tabs

Tabs need the `tablist`, `tab`, and `tabpanel` contract plus JavaScript for arrow-key, Home/End, and activation behavior. Without enhancement, keep the first panel visible and content usable.
